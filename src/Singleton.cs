using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Singleton : MonoBehaviour
{
    /*
    public static void SingletonSet<T>(T className, T classInstance, T classIndex)
    {
        if (classIndex == null)
        {
            classIndex = classInstance;
            if (classIndex == null) { Debug.Log($"{className}В™СґНЁВµВ№ВєВс"); }
        }
        else
        {
        }
    }
    */
}
